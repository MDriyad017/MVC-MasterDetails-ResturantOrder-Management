@model ev_01.Models.Food

<div class="row mb-2">
    <div class="col-10">
        @Html.DropDownListFor(x => x.FoodId, ViewBag.food as SelectList, "=== select food ===", htmlAttributes: new {@class = "form-select text-center"})
    </div>
    <div class="col-2">
        <a href="#" id="btnDelete" class="btn btn-danger"><i class="fa fa-close"></i></a>
    </div>
</div>
